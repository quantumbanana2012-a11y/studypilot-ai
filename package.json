{
  "name": "zentradeck-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Free AI study app with tutoring, games, review, file import, and hosted model support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
